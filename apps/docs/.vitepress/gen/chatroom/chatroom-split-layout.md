<!-- 由 scripts/gen-api-docs.mjs 自动生成，请勿手动编辑 -->
## ChatroomSplitLayout API

### Props

| 属性          | 类型                 | 默认值    | 说明                                           |
| --- | --- | --- | --- |
| memberWidth | `number \| string` | `280`  | 成员栏宽度（px 数字或 CSS 长度，缺省 280px；可拖拽调整）          |
| showMembers | `boolean`          | `true` | 是否渲染成员栏（场景 features.memberList !== 'none' 时） |

### Slots

| 插槽名       | 说明 |
| --- | --- |
| `stage`   | —  |
| `members` | —  |
